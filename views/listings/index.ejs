<% layout('layouts/boilerplate') %>

<style>
    #filters{
        display: flex;
        flex-wrap: wrap;
        align-items: center;
    }
    .filter{
        text-align: center;
        margin-right: 2rem;
        margin-top: 1.5rem;
        opacity: 0.7;
        color: inherit;
        text-decoration: none;
    }
    .filter:hover{
        opacity: 1;
        cursor: pointer;
        color: inherit;
    }
    .filter p {
        font-size: 0.8rem;
        margin-bottom: 0;
    }
    .selected-filter {
        opacity: 1;
        border-bottom: 2px solid black;
    }

    .tax-info{
        display: none;
    }
    .tax-toggle{
        border: 1px solid black;
        border-radius: 1rem;
        height: 3.25rem;
        padding: 1rem;
        margin-left: auto;
        display: flex;
        align-items: center;
    }
</style>

<div id="filters">
    <a href="/listings?category=Trending" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Trending') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-fire"></i></div>
        <p>Trending</p>
    </a>
    <a href="/listings?category=Rooms" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Rooms') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-bed"></i></div>
        <p>Rooms</p>
    </a>
    <a href="/listings?category=Iconic Cities" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Iconic Cities') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-mountain-city"></i></div>
        <p>Iconic Cities</p>
    </a>
    <a href="/listings?category=Mountain" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Mountain') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-mountain"></i></div>
        <p>Mountain</p>
    </a>
    <a href="/listings?category=Castles" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Castles') ? 'selected-filter' : '' %>">
        <div><i class="fa-brands fa-fort-awesome"></i></div>
        <p>Castles</p>
    </a>
    <a href="/listings?category=Amazing Pools" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Amazing Pools') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-person-swimming"></i></div>
        <p>Amazing Pools</p>
    </a>
    <a href="/listings?category=Camping" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Camping') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-campground"></i></div>
        <p>Camping</p>
    </a>
    <a href="/listings?category=Farms" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Farms') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-cow"></i></div>
        <p>Farms</p>
    </a>
    <a href="/listings?category=Arctic" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Arctic') ? 'selected-filter' : '' %>">
        <div><i class="fa-regular fa-snowflake"></i></div>
        <p>Arctic</p>
    </a>
    <a href="/listings?category=Domes" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Domes') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-landmark-dome"></i></div>
        <p>Domes</p>
    </a>
    <a href="/listings?category=Boats" class="filter <%= (typeof selectedCategory !== 'undefined' && selectedCategory === 'Boats') ? 'selected-filter' : '' %>">
        <div><i class="fa-solid fa-sailboat"></i></div>
        <p>Boats</p>
    </a>
    <div class="tax-toggle">
        <div class="form-check-reverse form-switch">
            <input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
            <label class="form-check-label" for="switchCheckDefault">Display total after taxes</label>
        </div>
    </div>
</div>    
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 g-4 mt-3">
    <% if (allListings.length === 0) { %>
        <div class="col-12 text-center my-5">
            <h4>No listings found!</h4>
            <p class="text-muted">Try searching for another location or category.</p>
            <a href="/listings" class="btn btn-outline-dark mt-2">View All Listings</a>
        </div>
    <% } %>
    <%for (let listing of allListings) { %> 
    <a href="/listings/<%= listing._id %>" class="listing-links">    
    <div class="card col listing-card" >
    <img src="<%= listing.image.url %>" 
    class="card-img-top" 
    alt="listing_image"
    style="height: 20rem;">
    <div class="card-img-overlay"></div>
    <div class="card-body">
        <p class="card-text">
           <b><%= listing.title %></b>  <br>
            &#8377; <%= listing.price.toLocaleString("en-IN") %>/night 
            <i class="tax-info">&nbsp; &nbsp; +18% GST</i>
            <br>
        </p>
    </div>
    </div>
    </a>
    <% }  %>
</div> 


<script>
    let taxtswitch = document.getElementById("switchCheckDefault");
    taxtswitch.addEventListener("click", ()=>{
        let Taxinfo = document.getElementsByClassName("tax-info");
        for(info of Taxinfo){
            if(info.style.display != "inline"){
                info.style.display = "inline";
            }
            else{
                info.style.display = "none";
            }
             
        }
    });
</script>